-- Автогенерировано dbc_to_lua.py из Spell.dbc (WotLK 3.3.5a)
-- SpellFamilyName = 1
-- Не редактируй вручную — перегенерируй скриптом
local M = {}

-- { id, name, rank, reqLevel, family }
M.spells = {
  { id=43735, name="Death Shout", rank=1, reqLevel=1, family=1 },
}

return M